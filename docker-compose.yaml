version: "3.8"

services:
  node:
    build:
      context: ./node
    image: javierfraga/utilcntr-node
    working_dir: /app
    stdin_open: true
    tty: true
    volumes:
      - .:/app

  python:
    build:
      context: ./python
    image: javierfraga/utilcntr-python
    working_dir: /app
    stdin_open: true
    tty: true
    volumes:
      - .:/app

  arch:
    build:
      context: ./arch
    image: javierfraga/utilcntr-arch
    working_dir: /mnt
    stdin_open: true
    tty: true
    volumes:
      - .:/mnt

  debian:
    build:
      context: ./debian
    image: javierfraga/utilcntr-debian
    working_dir: /mnt
    stdin_open: true
    tty: true
    volumes:
      - .:/mnt
